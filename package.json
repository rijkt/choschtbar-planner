{
  "scripts": {
    "deps": "yarn install",
    "watch": "shadow-cljs watch app;",
    "release": "shadow-cljs release app;",
    "server": "shadow-cljs server;",
    "clean": "rm -rf target; rm -rf public/js/compiled",
    "watch-css": "modular-styles watch --source src --dest src/choschtbar_planner/styles/ --files src/styles/",
    "build-css": "modular-styles compile --source src --dest src/choschtbar_planner/styles/ --files src/styles/",
    "start": "yarn watch-css & yarn watch"
  },
  "dependencies": {
    "create-react-class": "^15.6.2",
    "moment": "^2.29.1",
    "react": "^16.0.0",
    "react-big-calendar": "^0.38.1",
    "react-dom": "^16.0.0",
    "shadow-cljs": "^2.0.80"
  },
  "devDependencies": {
    "modular-styles": "^0.2.0",
    "postcss-import": "^12.0.1"
  }
}
